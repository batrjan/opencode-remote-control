node_modules/
dist/

# Built opencode web UI artifacts (copied from packages/app/dist; ~84MB of
# hashed assets regenerated by the UI build, not tracked in git).
public/*
!public/index.html
!public/join.html
